const React = require('react');

const Destination = () => {
  return (
    <div>
      <input placeholder='Where are you going?'/>
      <button></button>
    </div>
  )
}

module.exports = Destination;
